{
    "require": ["std:way"],
    "specs": [
        {
            "title": "Switch to given way",
            "input": ["ok", "FOO"],
            "flow": [["ok", "std:way", "other"]],
            "expected": ["other", "FOO"]
        },
        {
            "title": "Error on wrong type (array)",
            "input": ["ok", "FOO"],
            "flow": [["ok", "std:way", ["other"]]],
            "expected": ["error", null]
        }
    ]
}