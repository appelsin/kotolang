# kotolang-rb

Ruby implementation of Kotolang interpreter.

## Install

* Install Ruby
* `gem install bundler` or `sudo gem install bundler`
* `bundle install`

## Run tests

`ruby -Ilib:test test/*`